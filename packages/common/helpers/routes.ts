export enum QueryRoutes {
  convos = "conversations",
  later = "later",
  done = "done",
  drawer = "drawer",
  createConvo = "createConversation",
  search = "search",
}

export enum Routes {
  home = "/s",
}
