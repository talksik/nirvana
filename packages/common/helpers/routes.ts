enum Routes {
  convos = "/s#conversations",
  later = "/s#later",
  done = "/s#done",
  drawer = "/s#drawer",
  createConvo = "/s#createConversation",
  search = "/s#search",
}

export default Routes;
